{
  "name": "@forge/cli",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Forge coding CLI — multi-provider, BYO keys.",
  "bin": {
    "forge": "./dist/index.js"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "bun-types": "latest",
    "typescript": "^5.4.0"
  }
}
