{
  "name": "forge-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Forge — ADE + MCP + voice + CLI + bench + swarm, open-core.",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "desktop:dev": "pnpm --filter @forge/desktop tauri:dev",
    "desktop:build": "pnpm --filter @forge/desktop tauri:build",
    "api:dev": "pnpm --filter @forge/api dev",
    "mcp:dev": "pnpm --filter @forge/mcp dev",
    "cli:dev": "pnpm --filter @forge/cli dev",
    "web:dev": "pnpm --filter @forge/web dev",
    "spec:generate": "openapi-generator-cli generate -i spec/openapi.yaml -g typescript-fetch -o packages/sdk-ts/generated && openapi-generator-cli generate -i spec/openapi.yaml -g rust -o packages/sdk-rust/generated"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
